// Build settings. base './' makes the built game use relative paths, which
// is what itch.io and GitHub Pages need (they serve games from a subfolder,
// not the root of a website).
export default {
  base: './',
};
